# 🚀 Deploy Your Portfolio to GitHub Pages

## Steps (takes ~3 minutes)

### 1. Create your repo
- Go to **github.com** → click **New Repository**
- Name it exactly: `hpehlivantopcu.github.io`
- Check ✅ "Initialize with a README"
- Click **Create repository**

### 2. Upload the file
- Click **Add file** → **Upload file**
- Drag and drop `index.html` into the upload area
- Scroll down → commit message: `Add portfolio website`
- Click **Commit changes**

### 3. Enable GitHub Pages
- Go to your repo → **Settings** (gear icon, top right)
- Left sidebar → click **Pages**
- Under "Branch" → select `main`
- Under "Folder" → select `/ (root)`
- Click **Save**

### 4. Your site is live! 🎉
- URL: **https://hpehlivantopcu.github.io/**
- It may take 30–60 seconds to go live the first time

---

## 📝 Things to update before deploying

1. **Email** — Replace placeholder email with your real one (search for `hpehlivantopcu@gmail.com` and update if needed)
2. **LinkedIn URL** — Update `linkedin.com/in/hpehlivantopcu` with your real LinkedIn slug
3. **GitHub project links** — Each project card has a GitHub link. Update these with your actual repo URLs once you push project code
4. **Contact email** — The "Send a Message" button uses `mailto:` — make sure your email is correct

## 💡 Add your project repos
For each project, create a GitHub repo and push your code:
- `flu-simulation` → push the Flu Simulation notebooks
- `heart-disease-ml` → push the Heart Disease ML notebook  
- `churn-analysis` → push your churn analysis code
- `Stock-Miner` → push the StockMiner app code
- `BuzzCars` → push the BuzzCars project files
